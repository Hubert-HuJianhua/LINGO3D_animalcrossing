import store, { createEffect, GetGlobalState, SetGlobalState } from "."

export default class Reactive<T> {
    public set: SetGlobalState<T>
    public get: GetGlobalState<T | undefined>
    
    public constructor(value?: T) {
        [this.set, this.get] = store(value)
    }

    public static createEffect = createEffect
}