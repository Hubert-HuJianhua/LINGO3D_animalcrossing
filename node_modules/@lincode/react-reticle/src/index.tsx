import React from "react"
//@ts-ignore
import css from "../assets/style.module.css"

const Reticle: React.FC<React.HTMLAttributes<HTMLDivElement>> = ({ className, ...props }) => {
    return (
        <div {...props} className={css.center + " " + (className ?? "")}>
            <div style={{
                backgroundColor: "#999999",
                width: 20,
                height: 20,
                borderRadius: 9999,
                border: "1px solid white"
            }} />
        </div>
    )
}

export default Reticle
