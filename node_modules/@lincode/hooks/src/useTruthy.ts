import { useRef } from "react"

export default <T>(value: T, condition?: boolean) => {
    const ref = useRef<T>()
    
    if (condition === undefined)
        value && (ref.current = value)
    else
        condition && (ref.current = value)

    return ref.current
}