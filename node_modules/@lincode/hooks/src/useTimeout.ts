import { useEffect } from "react"

export default (cb: () => void, duration: number, deps?: Array<any>) => {
    useEffect(() => {
        const timeout = setTimeout(cb, duration)
        return () => {
            clearTimeout(timeout)
        }
    }, deps ?? [])
}