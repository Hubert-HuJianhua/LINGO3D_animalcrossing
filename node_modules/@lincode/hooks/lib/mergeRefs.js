export default (...inputRefs) => (ref) => {
    for (const inputRef of inputRefs)
        if (typeof inputRef === "function")
            inputRef(ref);
        else
            inputRef.current = ref;
};
