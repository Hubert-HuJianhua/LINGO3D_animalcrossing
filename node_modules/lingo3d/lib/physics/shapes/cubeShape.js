import { Box } from "cannon-es";
import getActualScale from "../../display/utils/getActualScale";
export default (target) => {
    target.physicsBody.addShape(new Box(getActualScale(target).multiplyScalar(0.5)));
};
