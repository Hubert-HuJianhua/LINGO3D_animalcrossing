import * as SkeletonUtils from "three/examples/jsm/utils/SkeletonUtils";
export default (target, animations = target.animations) => {
    //@ts-ignore
    const clone = SkeletonUtils.clone(target);
    clone.animations = animations;
    return clone;
};
