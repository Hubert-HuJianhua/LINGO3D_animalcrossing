import { SpotLight as ThreeSpotLight } from "three";
import LightBase from "../core/LightBase";
import PointLightMixin from "../core/mixins/PointLightMixin";
import ISpotLight from "../../interface/ISpotLight";
declare class SpotLight extends LightBase<ThreeSpotLight> implements ISpotLight {
    constructor();
    get angle(): number;
    set angle(val: number);
    get penumbra(): number;
    set penumbra(val: number);
}
interface SpotLight extends LightBase<ThreeSpotLight>, PointLightMixin<ThreeSpotLight> {
}
export default SpotLight;
