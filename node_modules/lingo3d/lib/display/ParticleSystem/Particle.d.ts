import { Texture } from "three";
export declare const createParticle: () => Particle;
export default class Particle {
    private material;
    private sprite;
    private startTime;
    private accelX;
    private accelY;
    private accelZ;
    private speedX;
    private speedY;
    private speedZ;
    private lifeTime;
    private scaleStart;
    private sizeRange;
    private opacityStart;
    private opacityRange;
    private rStart;
    private rRange;
    private gStart;
    private gRange;
    private bStart;
    private bRange;
    init(texture: Texture, bloom: boolean, size: number, sizeEnd: number | undefined, opacity: number, opacityEnd: number | undefined, color: string, colorEnd: string | undefined, worldX: number, worldY: number, worldZ: number, accelX: number, accelY: number, accelZ: number, speedX: number, speedY: number, speedZ: number, lifeTime: number): void;
    update(): void;
    dispose(): void;
}
