import { Group as ThreeGroup } from "three";
import ObjectManager from "./core/ObjectManager";
import fitContent from "./utils/fitContent";
export default class Group extends ObjectManager {
    constructor() {
        super(new ThreeGroup());
    }
    append(target) {
        super.append(target);
        fitContent(this);
    }
    attach(target) {
        super.attach(target);
        fitContent(this);
    }
}
