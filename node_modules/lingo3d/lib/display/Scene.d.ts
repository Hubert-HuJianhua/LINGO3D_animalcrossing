import Loaded from "./core/Loaded";
import { SceneGraphNode } from "./utils/deserialize/types";
import IScene from "../interface/IScene";
export default class Scene extends Loaded<Record<string, any> | Array<any>> implements IScene {
    protected load: (url: string) => Promise<any[] | Record<string, any>>;
    private setupNode?;
    applySetup(): void;
    protected resolveLoaded(data: Array<SceneGraphNode>): void;
}
