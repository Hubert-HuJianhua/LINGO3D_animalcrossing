import ObjectManager from "../core/ObjectManager";
import Camera from "./Camera";
import IThirdPersonCamera from "../../interface/IThirdPersonCamera";
export default class ThirdPersonCamera extends Camera implements IThirdPersonCamera {
    constructor();
    protected mouseControlMode: 0 | 1;
    private _loopHandle0;
    private _target;
    get target(): ObjectManager | undefined;
    set target(target: ObjectManager | undefined);
    append(object: ObjectManager): void;
}
