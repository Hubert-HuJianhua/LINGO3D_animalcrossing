import { Disposable } from "@lincode/promiselikes";
import { Object3D, AnimationClip } from "three";
import { AnimationData } from "../../utils/deserialize/types";
import AnimationItem from "./AnimationItem";
export declare type PlayOptions = {
    crossFade?: number;
    repeat?: boolean;
    onFinish?: () => void;
};
export default class AnimationManager extends Disposable {
    private clip?;
    name: string;
    private mixer;
    private action?;
    constructor(nameOrClip: string | AnimationClip, target: AnimationItem | Object3D);
    dispose(): this;
    get duration(): number;
    private loadClip;
    setTracks(data: AnimationData): void;
    play({ crossFade, repeat, onFinish }?: PlayOptions): void;
    stop(): void;
    update(seconds: number): void;
}
