import EventLoopItem from "../../../api/core/EventLoopItem";
import AnimationManager, { PlayOptions } from "./AnimationManager";
import IAnimation, { AnimationValue } from "../../../interface/IAnimation";
export default abstract class AnimationItem extends EventLoopItem implements IAnimation {
    protected animationManagers?: Record<string, AnimationManager>;
    get animations(): Record<string, AnimationManager>;
    set animations(val: Record<string, AnimationManager>);
    createAnimation(name: string): AnimationManager;
    protected animationPromises?: Array<Promise<void>>;
    private animationManager?;
    playAnimation(name?: string, o?: PlayOptions): Promise<void>;
    stopAnimation(): void;
    private _animation?;
    private _animationProxy?;
    get animation(): AnimationValue;
    set animation(val: string | AnimationValue | undefined);
}
