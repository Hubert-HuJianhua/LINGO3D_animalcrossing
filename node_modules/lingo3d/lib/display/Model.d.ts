import { Group } from "three";
import Loaded from "./core/Loaded";
import AnimationManager from "./core/SimpleObjectManager/AnimationManager";
import IModel from "../interface/IModel";
export default class Model extends Loaded<Group> implements IModel {
    loadAnimation(url: string, name?: string): void;
    get animations(): Record<string, AnimationManager>;
    set animations(val: Record<string, string | AnimationManager>);
    protected load(url: string): Promise<Group>;
    private _loadedScale?;
    get loadedScale(): number;
    set loadedScale(val: number);
    private _loadedPos?;
    private _loadedX?;
    get loadedX(): number;
    set loadedX(val: number);
    private _loadedY?;
    get loadedY(): number;
    set loadedY(val: number);
    private _loadedZ?;
    get loadedZ(): number;
    set loadedZ(val: number);
    protected resolveLoaded(loadedObject3d: Group): void;
}
